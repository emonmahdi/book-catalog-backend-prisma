export type IBookFilterRequest = {
  // page?: number;
  // size?: number;
  // sortBy?: string;
  // sortOrder?: 'asc' | 'desc';
  minPrice?: number;
  maxPrice?: number;
  category?: string;
  searchTerm?: string;
};

// searchTerm?: string | undefined;
// categoryId?: string | undefined;
// minPrice?: number;
// maxPrice?: number;
